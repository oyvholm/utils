#!/bin/sh

# $Id: cunw,v 1.3 1999/05/20 03:27:13 sunny Exp $
# Lister ut alle filer som ikke er registrert i CVS (Cvs-UNWanted)

# BUGS: Avhengig av utskriftsformatet til cvs(1)

# Nedenfor her så var det en "cut -c 50-" på begge to. Fjerna i rev. 1.3
# FIXME: Nødvendig å ta dette to ganger?
(cvs -n stat `find . -type d | grep -v -e 'CVS$'` >/dev/null) 2>&1
(cvs -n stat `find . -type f | grep -v -e 'CVS/Entries$' -e 'CVS/Repository$' -e 'CVS/Root$' ` >/dev/null) 2>&1
