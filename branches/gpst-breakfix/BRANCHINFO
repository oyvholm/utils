$Id$

Changes compared to trunk:

  $found_move changed to $Dat{'break'}.

Tests failing here, but works in trunk:

  "../gpst --chronology chronology-error.gpsml"
  Check for chronology errors and duplicated times
    Missing a <break/> on both sides of the faulty trackpoint.

  "../gpst --fix --chronology chronology-error.gpsml"
  Remove bad timestamps
    It creates two <break/>s in front of the faulty trackpoint. One of the 
    <break/>s shall appear after the misplaced point.

  "../gpst --pos1 2.11,2.12 --pos2 3.31,3.32 --inside multitrack-pause.gpx"
  Check --inside option (gpx to gpst)
    Three <break/>s missing: One after <title>track1</title> and one on each 
    side of the faulty trackpoint.

  "../gpst multitrack.gpx"
  Read GPX file with multiple tracks
    Missing three <break/>s: In front of the "Track 2", "Track 3" and "Track 
    4" titles.

  "../gpst multitrack-pause.gpx"
  Should be equal to multitrack-pause.gpsml
    Missing 4 <break/>s.

  "../gpst -o gpx comments.mayko"
  Output GPX from Mayko file with commented-out lines
    Should be splitted with </trkseg></trkseg>, comments.mayko contains a "# 
    move".

  "../gpst -o xgraph multitrack.gpx"
  Output xgraph format from GPX
    Missing three "move"s.

  "../gpst --pos1 2.11,2.12 --pos2 3.31,3.32 --outside multitrack-pause.gpx"
  Check --outside option (gpx to gpst)
    Missing 3 <break/>s.

  "../gpst -re multitrack.gpx"
  Require elevation from GPX data
  Insert <pause> between gpx tracks
    Missing 4 <break/>s.

  "../gpst -t multitrack-pause.gpx"
  Missing 4 <break/>s.

  "../gpst -t multitrack-pause.gpsml"
  Insert <pause> between gpsml titles
    Missing 4 <break/>s.

  "../gpst -o gpx -w comments.mayko"
  Output whitespace-stripped GPX from Mayko file with commented-out lines
    Missing separating </trkseg><trkseg>.

vim: set fo=tcqw tw=78 ts=2 sw=2 sts=2 :
