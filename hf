#!/usr/bin/perl

# $Id: hf,v 1.3 1999/03/03 18:36:43 sunny Exp $

use strict;

$| = 1;

my $Tabs = "";
my $tab_indent = 0;
my $orig_line = "";
my @Elements = ("applet", "blockquote", "body", "center", "div", "dl", "form", "frameset", "head", "html", "ol", "map", "noframes", "script", "style", "table", "td", "tr", "ul");

while (<>) {
	my $Line = $_;
	my $Element = "";
	my $f = "";

	$Line =~ s/^\t+(.*)/$1/; # Hiv alle TAB'er i begynnelsen til helsike, spacer rører vi ikke.
	for $f (@Elements) {
		if ($Line =~ m!</($f)([ >])!i) { # Hvis end-tag ble funnet...
			$Element = $1;
			$tab_indent--;
			$Tabs =~ s/\t$//;
			# $Line =~ s/($Element)/sprintf("\n%s%s", $Tabs, $1)/gei;
		}
	}
	my $out_line = sprintf("%s%s", $Tabs, $Line);
	$out_line =~ s/\s*$//g; # Fjern alle whitespace på slutten
	print "$out_line\n";
	for $f (@Elements) {
		if ($Line =~ m!<($f)([ >])!i) { # Hvis begin-tag ble funnet...
			$Element = $1;
			$tab_indent++;
			$Tabs .= "\t";
			# $Line =~ s/($Element)/sprintf("\n%s%s", $Tabs, $1)/gei;
		}
	}
}

#les_linje;
#stripp tab i begynnelsen
#skriv tab
#skriv linje

#### End of file $Id: hf,v 1.3 1999/03/03 18:36:43 sunny Exp $ ####
